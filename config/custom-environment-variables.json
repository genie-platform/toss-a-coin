{
  "api": {
    "allowCors": "GAME_API_ALLOW_CORS",
    "secret": "GAME_API_SECRET",
    "tokenExpiresIn": "GAME_TOKEN_EXPIRES_IN",
    "port": "PORT"
  },
  "aws": {
    "secrets": {
      "secretId": "GAME_AWS_SECRET_ID",
       "manager": {
        "region": "GAME_AWS_SECRET_MANAGER_REGION"
      }
    }
  },
  "mongo": {
    "uri": "GAME_MONGO_URI",
    "debug": "GAME_MONGO_DEBUG"
  },
  "secrets": {
    "accounts": {
      "seed": "GAME_SECRETS_ACCOUNTS_SEED"
    }
  },
  "crypto": {
    "jwt": "GAME_CRYPTO_JWT"
  }
}
